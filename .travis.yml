languaje: python
python:
  - "2.7"
env:
  - DJANGO_VERSION=1.8.5
install:
  - sudo apt-get install python-dev
  - sudo pip install --upgrade pip
  - sudo pip install Django 
script:
  - cd reyic/IvApp/AppIV/ProyectoIV;
  - sudo python setup.py install
  - sudo python manage.py test

